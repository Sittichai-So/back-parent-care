services:
  mongo:
    image: mongo:7
    container_name: parent-mongo
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secret_parent
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
