node_modules/
.env
.env.*
!.env.example
uploads/
logs/

# Local dev docker setup (has the Mongo root user/password inline) —
# keep it out of the repo instead of committing credentials.
docker-compose.yml
docker-compose*.yml
